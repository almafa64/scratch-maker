namespace Scratch
{
	public enum Goto
	{
		RandomPosition,
		MousePointer,
		Sprite
	}

	public enum Towards
	{
		MousePointer,
		Sprite
	}

	/*public class Sprite
	{
		public class Column
		{
			public class _Motion
			{
				public string Move(double steps)
				{
					string id = ID.Make();
					return id;
				}

				public string TurnRight(double degrees)
				{
					string id = ID.Make();
					return id;
				}

				public string TurnLeft(double degrees)
				{
					string id = ID.Make();
					return id;
				}

				public string Goto(Goto where, Sprite sprite = null)
				{
					string id = ID.Make();
					return id;
				}

				public string Goto(double x, double y)
				{
					string id = ID.Make();
					return id;
				}

				public string Glide(double seconds, Goto where, Sprite sprite = null)
				{
					string id = ID.Make();
					return id;
				}

				public string Glide(double seconds, double x, double y)
				{
					string id = ID.Make();
					return id;
				}

				public string PointInDirection(double direction)
				{
					string id = ID.Make();
					return id;
				}

				public string PointTowards(Towards to, Sprite sprite = null)
				{
					string id = ID.Make();
					return id;
				}

				public string ChangeX(double x)
				{
					string id = ID.Make();
					return id;
				}

				public string SetX(double x)
				{
					string id = ID.Make();
					return id;
				}

				public string ChangeY(double y)
				{
					string id = ID.Make();
					return id;
				}

				public string SetY(double y)
				{
					string id = ID.Make();
					return id;
				}

				public string BounceOnEdge()
				{
					string id = ID.Make();
					return id;
				}

				public string SetRotationStyle(RotationStyle rotationStyle)
				{
					string id = ID.Make();
					return id;
				}

				public string GetX()
				{
					string id = ID.Make();
					return id;
				}

				public string GetY()
				{
					string id = ID.Make();
					return id;
				}

				public string GetDirection()
				{
					string id = ID.Make();
					return id;
				}
			}
		}
	}*/
}